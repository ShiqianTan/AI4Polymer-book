# 第 3 章概念边界收紧：修改记录

> 日期：2026-09-17 · 范围：`manuscripts/03-数据、基准与可复现性.md`、`manuscripts/ch03/build.py`、`archive/outlines/03-…md`、`archive/outlines/extensions/03-…md`
> 格式：位置 → 原句/图 → 问题 → 改法 → 优先级

## A. 证据标签升级（六级）

| 位置 | 原句/图 | 问题 | 改法 | 优先级 |
| --- | --- | --- | --- | --- |
| 3.1.3（WoS 检索量、逐年量、91.9%） | 「得到 3178 条记录 `[文献]`；年发文量…`[文献]`」 | WoS 检索是本项目对公开数据集的再分析，不是文献报告值；旧四级标签没有对应档位 | 改为 `[公开实验数据]`（公开数据集再分析） | P1 |
| 3.1.3（缺失率 24.0%/14.5%） | 「缺失率分别为 24.0% 与 14.5% `[文献]`」 | 同上，是本书对检索语料的统计 | 改为 `[公开实验数据]` | P1 |
| 3.11.1 | 「学习曲线参数…`[文献]`」 | 参数是声明输入，不是文献值；与 3.3.1 和速查表不一致 | 改为 `[示意]` | P1 |
| 本章数字速查（表头前） | 无 | 正文出现 `[公开实验数据]`，但本章未定义六级标签 | 新增六级标签定义段：`[实验]/[公开实验数据]/[模拟]/[复算]/[文献]/[示意]`，并写明公开数据重跑属 `[公开实验数据]` 或 `[复算]`、不是 `[实验]` | P0 |
| 图 3-1 图注 | 「（证据：文献）」 | 图混合了公开数据再分析与文献值 | 改为「（证据：公开实验数据／文献）」 | P1 |
| 速查表 `证据` 列 | 24.0%、14.5% 行标「文献」 | 同上 | 改为「公开实验数据」；「对照表来源类别」由「文献」改为「示意」（本书归纳） | P1 |
| 3.12.3 可复现 Box | 「标 `[文献]` 的数字回到已归档来源…」 | 未覆盖新标签 | 增补「标 `[公开实验数据]` 的数字回到公开数据集与重分析记录」 | P1 |

注：WoS 为文献计量库而非实验数据；此处按「公开数据集再分析」的语义使用 `[公开实验数据]`。附录 E 的 N12–N14 仍为 `[文献]`，跨章同步留待附录统一（本次按硬性约束未改其他章节）。

## B. P0-1：四类区分与图 3-5 重做

| 位置 | 原句/图 | 问题 | 改法 | 优先级 |
| --- | --- | --- | --- | --- |
| 3.8 引言 | 「这一节把高分子数据的泄漏拆成六类」 | 把 homolog/scaffold/monomer overlap 一律叫泄漏，同族内插是合法 evaluation regime | 改为先分四类（Direct leakage / Duplicate contamination / Chemical overlap / Distribution shift），再说明是否一定错误 | P0 |
| 3.8.1 | 六条 bullet 平铺，组成泄漏用 `|r|>0.95` 判据 | 缺少「是否错误取决于任务」的层次；相关系数判据错误（见 D） | 保留「六类泄漏」标题与 $L_k$ 公式，按四类分组重排；同系物/单体对/主链明确写「取决于任务」 | P0 |
| 3.8.1 预算段 | 「任意一类不为零，测试误差就被那一类污染」 | 对化学重叠与分布偏移不成立 | 改为「逐类报告并注明属于四类中的哪一档」 | P0 |
| 3.8.1 处理顺序段 | 「先修组成泄漏…再修相同重复单元泄漏…最后修其余四类」 | 把化学重叠一律当泄漏处理 | 改为先直接泄漏、再重复污染、最后化学重叠；化学重叠是否压到零取决于部署单元；分布偏移不靠换键消除 | P0 |
| 图 3-5 | 六格「六类数据泄漏」 | 类别混淆，且仅靠内容而非禁止/取决/偏移分层 | 重做为四格：红实线=禁止（Direct leakage、Duplicate contamination）、橙虚线=取决于任务（Chemical overlap）、灰点线=泛化任务（Distribution shift）；线型+处置标签与颜色并用 | P0 |
| 图 3-5 图注 | 「…读法：先确认任务依赖哪一类键…」 | 违反「图注只写展示什么+来源/假设，读法放正文」 | 删读法，改为「图展示什么 + 来源/假设」；读法移入 3.8.1 正文 | P0 |
| 3.3.3 | 「三类泄漏各有来源…同系物泄漏…误差被系统性低估」 | 3.3.3 与 3.8 的口径不一致 | 改为三类重叠（duplicate/homolog/target leakage），同系物注明取决于部署目标 | P0 |
| 3.8.2 流程 | 「阈值告警：对任务依赖的键设阈值」 | 未区分硬约束与任务依赖 | 增特征可获得性审计；直接泄漏/重复污染要求为零，化学重叠按任务，分布偏移不作泄漏处理 | P1 |
| 3.11.4、本章小结、谬误 | 「泄漏率接近 1」「泄漏等价于数据膨胀」「去重之后就没有泄漏」 | 术语与四类框架不一致 | 改为「同系物重叠率」「在声明外推任务下重叠等价于数据膨胀」；去重只消除重复污染 | P1 |

## C. P0-2：单位换算与标准化分开

| 位置 | 原句/图 | 问题 | 改法 | 优先级 |
| --- | --- | --- | --- | --- |
| 3.7.3 | 「先划分再统一单位会把测试集的单位分布泄漏进训练」 | 错误：$T(\mathrm{K})=T(^\circ\mathrm{C})+273.15$ 是确定性物理变换，不从数据估参，可在 split 前统一 | 删除该句；改为区分确定性变换（规范化、单位换算、确定性解析）与需拟合变换（插补器、缩放、PCA、特征选择、超参搜索），后者只在训练集 fit | P0 |
| 3.7.3（新增图 3-9） | 无 | 缺少一张把安全顺序讲清的图 | 新增 `figure-3-9-safe-order`：上=划分前确定性变换，下=划分后仅在训练集拟合，测试集只 transform；编号接在图 3-8 之后 | P0 |
| 3.3.2 | 「换算因子取自各单位的定义 `[文献]`…」 | 未说明为何单位换算安全而缩放不安全 | 增补：换算因子是确定性常数、可在划分前统一；均值–方差缩放/PCA/插补统计量只在训练集拟合 | P1 |
| 3.13.1 | 「九步：…处理缺失…划分」 | 把插补放在划分前，与 3.7.3 矛盾 | 改为两段：确定性处理在划分前；建键、划分、拟合插补/缩放在划分后 | P0 |
| 图 3-9 图注 | — | — | 只写展示什么（按是否估参分类，与颜色无关）+ 证据标签 | P1 |

## D. P0-3：删除 `|r|>0.95` 判据

| 位置 | 原句/图 | 问题 | 改法 | 优先级 |
| --- | --- | --- | --- | --- |
| 3.8.1 组成泄漏 | 「检测方法是计算每个特征与目标的相关系数，标记 $|r|>0.95$ 的特征」 | 高相关≠泄漏；物理有意义的变量可与 target 高相关 | 删除；改为可获得性判据（预测时是否可得、是否由 target/未来信息构造）；相关性降级为 audit warning | P0 |
| 3.8.2 流程第 4 步 | 「阈值告警」 | 同上 | 增「特征审计」；相关系数只作提示 | P1 |

## E. 数据集表出版级核验

| 位置 | 原句/图 | 问题 | 改法 | 优先级 |
| --- | --- | --- | --- | --- |
| 3.6.1 主表 | 「PoLyInfo `1.2×10⁴` 条 / PI1M `10⁶` 条 / POINT2 `2.4×10⁵` 条」 | 计量对象不同（polymer entries / structures / entries），规模不可直接横比 | 表头改「规模（计量对象）」，逐行写明对象（聚合物条目、生成结构、数据库条目、模拟条目、专利文档、实验记录等） | P0 |
| 3.6.1 主表 OMG 行 | 「未核实」 | 裸「未核实」，无原因 | 改为「本书未核实（公开规模未复核）」，末列「规模待补」 | P0 |
| 3.6.1（新增元数据表） | 无 | 缺少 version / access date / DOI or URL / license | 新增 8 行登记表（PoLyInfo、Polymer Genome、PI1M、OMG、POINT2、SMiPoly、polyBERT、WoS），字段取自 `datasets.json` 与 `references/`；无法核对的写「本书未核实（原因）」 | P0 |
| 3.6.1 正文 | 「未核实的保持空缺（记为未核实）」 | 同上 | 改为「本书未核实并说明原因」；补计量对象说明 | P1 |
| `[^datasets]` 脚注 | 「规模保持未核实状态」 | 裸「未核实」 | 改为「公开规模本书未核实（未复核到可归档的计数口径）」 | P1 |

已核实/未核实清单：
- 已核实：OMG 许可 GPL-3.0（归档 README）；polyBERT 论文 CC BY 4.0（归档 PDF），语料声明仅限学术用途；POINT2 版本 `2503.23491v1`；各来源 DOI/URL 与访问日期（`references/sources.tsv`、`references/manifest.json`）。
- 本书未核实：PoLyInfo、Polymer Genome、PI1M、POINT2、SMiPoly 的许可条款（归档快照未含）；WoS 订阅条款；OMG 公开规模。均在表中给出原因，未编造。

## F. 绝对化语言

| 位置 | 原句 | 改法 | 优先级 |
| --- | --- | --- | --- |
| 3.3.1 / 本章小结 | 「目标 0.1 就永远无法达到」「使目标无法达到」 | 「在该幂律模型下不可达」 | P1 |
| 3.3.1 | 「字段缺失的记录无法进入需要条件的任务」 | 「通常无法进入」 | P2 |
| 3.10.3 | 「只有固定划分与固定指标，跨研究的数字才能放进同一张表」 | 「在固定划分与固定指标下…才能」 | P1 |
| 3.11.3 | 「只要两种划分的误差差超过零，就值得给出 $f$」 | 「误差差超过种子波动时，通常值得给出 $f$」 | P2 |
| 3.9.3 | 「模型只能外推」 | 「模型主要靠外推」 | P2 |

## G. 同步与验证

| 位置 | 改法 | 优先级 |
| --- | --- | --- |
| `manuscripts/ch03/build.py` | 重写 `figure_leakage_types`（四类、线型+标签）；新增 `figure_safe_order`；更新 `figure-index.json` 顺序 | P0 |
| `archive/outlines/03-…md` | 同步 3.6.1/3.7.3/3.8/3.13.1 描述；新增「图 3-9」块；重写图 3-5 块 | P0 |
| `archive/outlines/extensions/03-…md` | 同步 3.3.2/3.3.3/3.6.1/3.7.3/3.8.1/3.11.4/3.13.1；实验 3-6 改「六类重叠检测」 | P0 |

验证命令与结果见本章交付报告；`figure-index.json` 与正文图片顺序一致（3-9 位于 3.7.3，排在 3-4 之后）。
